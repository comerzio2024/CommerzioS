/**
 * Reviews Routes (Stub)
 * Placeholder for review-related endpoints
 */

import { Express } from "express";

export function registerReviewsRoutes(app: Express): void {
    // TODO: Migrate review endpoints from routes.ts
    // - GET /api/reviews
    // - POST /api/services/:id/reviews
    // - PATCH /api/reviews/:id
}
