/**
 * Services Routes (Stub)
 * Placeholder for service-related endpoints
 */

import { Express } from "express";

export function registerServicesRoutes(app: Express): void {
    // TODO: Migrate service endpoints from routes.ts
    // - GET /api/services
    // - GET /api/services/:id
    // - POST /api/services
    // - PATCH /api/services/:id
    // - DELETE /api/services/:id
}
